# Screenshots & media

Add guest-safe images or short recordings here (e.g. `hero.png`, `itinerary.png`). Use the live site only with **cropped** or **non-PII** views—avoid guest names, emails, room numbers, or inbox threads in pixels.

Reference them from the Markdown guides with: `![Alt]({{ site.baseurl }}/assets/your-file.png)`  
(or a path that works with your Jekyll `baseurl`).
